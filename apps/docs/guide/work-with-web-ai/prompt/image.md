# Prompt to Image

Prompt to Image creates an image asset from the prompt bar and places it into the active slide.

## Controls

The AI Tools panel owns image generation setup such as model readiness, size presets, steps, and seed controls where available.

> WIP
> AI image generation requires model availability. Capture this after the guided AI setup path is stable.
